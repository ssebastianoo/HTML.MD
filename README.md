# HTML.MD

Convert the current page to markdown and send it to an ai agent.

> Made using [wxt](https://wxt.dev/)

### Develop
```
bun run dev
```

### Build
```
bun run build
bun run build:firefox
```
Note that you can use `npm` instead of `bun`
